{
  "name": "unriddle",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "turbo run format"
  },
   "packageManager": "yarn@1.22.22",
  "devDependencies": {
    "turbo": "^2.3.0"
  }
}
